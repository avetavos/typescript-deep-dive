---
title: 'this Types & Polymorphism'
description: 'polymorphic this type สำหรับ fluent chaining, this parameter และการเสีย this binding แสดงออกใน type system อย่างไร'
sidebar:
  order: 3
---

import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## `this` ก็เป็น type ด้วย

ภายใน class `this` ไม่ได้เป็นแค่ value — เป็น **type** ที่หมายถึง "type ของ instance ปัจจุบัน" และที่สำคัญคือใน subclass `this` หมายถึง *subclass* ไม่ใช่ class ที่ method นั้นถูกเขียนไว้ นี่คือ **polymorphic `this` type** และทำให้ fluent API ทำงานถูกต้องข้าม inheritance

## fluent chaining ด้วย polymorphic `this`

builder ที่ return `this` จากแต่ละ method ทำให้ call ต่อกันเป็น chain ได้ การ type return เป็น `this` (ไม่ใช่ชื่อ class) แปลว่า method ของ subclass ยัง chain ต่อได้

```ts
class QueryBuilder {
  private parts: string[] = [];
  where(cond: string): this {   // return type is `this`, not QueryBuilder
    this.parts.push(cond);
    return this;
  }
}

class MySQLBuilder extends QueryBuilder {
  limit(n: number): this {
    return this;
  }
}

new MySQLBuilder()
  .where("a = 1") // returns MySQLBuilder (because of `this`), so...
  .limit(10);     // ...limit() is still available. ✅
```

ถ้า `where` ถูก type ให้ return `QueryBuilder` chain จะ "ลืม" ว่าเป็น `MySQLBuilder` และ `.limit()` จะ fail polymorphic `this` รักษา type จริงไว้ตลอด chain

## `this` parameter

function สามารถ declare parameter ตัวแรกปลอม ๆ ชื่อ `this` เพื่อ type ว่า `this` ต้องเป็นอะไรตอน function ถูกเรียก parameter นี้ถูก erase ตอน emit และไม่กระทบ argument list จริง มีไว้ type-check calling context ล้วน ๆ

```ts
interface Card { title: string; }

function render(this: Card) {
  return this.title.toUpperCase();
}

// render();            // ❌ The 'this' context of type 'void' is not assignable...
render.call({ title: "hi" }); // ✅ this is a Card here
```

อันนี้จับ bug ทั้งกลุ่มที่ method พึ่ง `this` ที่อาจไม่ได้รับจริง

## pitfall เรื่องเสีย `this` ใน type

footgun คลาสสิกของ JavaScript — ส่ง method เป็น callback แล้วเสีย `this` — มีเรื่องราวระดับ type ด้วย เมื่อคุณส่ง `obj.method` แบบ by reference `this` binding จะหลุด

```ts
class Counter {
  count = 0;
  increment() { this.count++; } // relies on `this`
}

const c = new Counter();
const fn = c.increment;
fn(); // 💥 at runtime `this` is undefined — and the type system says nothing
```

โดย default type system **ไม่จับ** อันนี้เลย: `increment` ไม่เคยประกาศว่า `this` ต้องเป็นอะไร ดังนั้น `fn()` type-check ผ่านโดยไม่มี diagnostic เลยแม้จะพังตอน runtime `strictBindCallApply` ไม่ได้เพิ่มเช็คนี้ให้ — มันแค่ทำให้ `.call`/`.apply`/`.bind` เองตรวจ argument `this` ของมันกับ `this` parameter ที่ method ประกาศไว้แล้ว ถ้าอยากได้ warning ตอน compile ต้องให้ method ประกาศ `this` ที่ต้องการเองตรง ๆ:

```ts
// @expect-error TS2684
class Counter {
  count = 0;
  increment(this: Counter) { this.count++; }
}

const c = new Counter();
const fn = c.increment;
fn();
```

ตอนนี้ mismatch ถูกจับแล้ว: `TS2684: The 'this' context of type 'void' is not assignable to method's 'this' of type 'Counter'.` วิธีแก้แบบ idiomatic คือใช้ **arrow-function field** (`increment = () => { ... }` ซึ่ง bind `this` แบบ lexical) หรือ bind ที่จุดเรียก (`c.increment.bind(c)`) แต่ arrow field ก็ไม่ได้ฟรี — มันถูกสร้างใหม่บน **ทุก instance** แทนที่จะอยู่บน prototype ตัวเดียวเหมือน method ปกติ จึงกิน memory ต่อ instance มากกว่า และเพราะมันเป็น field ธรรมดาไม่ใช่ method subclass จึง `override` มันไม่ได้และเรียกผ่าน `super` ไม่ได้ด้วย

<Callout title="สรุปหนึ่งบรรทัด">
`this` เป็น type ที่หมายถึง "instance ปัจจุบัน" และเป็น **polymorphic** — ใน subclass จะหมายถึง subclass return `this` (ไม่ใช่ชื่อ class) จาก builder method เพื่อให้ chain รักษา subtype จริงไว้ `this` **parameter** type calling context ที่ต้องการและถูก erase ตอน emit bug เสีย `this` ของ JavaScript type checker มองไม่เห็นเลยเว้นแต่ method จะประกาศ `this` parameter — arrow-function field หรือ `.bind()` แก้ได้ แลกกับ copy ต่อ instance ที่ `override` ไม่ได้
</Callout>

export const quizThis = [
  {
    q: 'type `this` ภายใน method ของ class หมายถึงอะไร?',
    options: [
      'class ที่ method ถูกนิยามเป๊ะ ๆ เสมอ',
      'type ของ instance ปัจจุบัน — ใน subclass คือ type ของ subclass (polymorphic)',
      'global object',
      'any type พิเศษ',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมต้อง return `this` (แทนชื่อ class) จาก builder method ที่ chain ได้?',
    options: [
      'เร็วกว่า',
      'เพื่อให้ chain รักษา subtype จริงไว้และ method ของ subclass ยังเรียกได้',
      'เป็น syntax ที่บังคับ',
      'เพื่อเลี่ยง inheritance',
    ],
    answer: 1,
  },
  {
    q: '`this` parameter ใช้ทำอะไร?',
    options: [
      'ส่ง argument runtime เพิ่มอีกตัว',
      'type-check calling context ที่ต้องการ ถูก erase และไม่ใช่ argument จริง',
      'ทำให้ method เป็น static',
      'เปลี่ยนชื่อ instance',
    ],
    answer: 1,
  },
  {
    q: 'วิธีแก้แบบ idiomatic เมื่อ method เสีย `this` ตอนถูกส่งเป็น callback คืออะไร?',
    options: [
      'ทำให้ method เป็น static',
      'ใช้ arrow-function class field หรือ bind ที่จุดเรียกด้วย `.bind()`',
      'เปลี่ยนชื่อ method',
      'ไม่มีทางแก้',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="classes-and-oop/this-types-and-polymorphism" questions={quizThis} />

<ProgressTracker client:visible id="classes-and-oop/this-types-and-polymorphism" />
